---
interface Props {
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  variant?: 'primary' | 'secondary' | 'outline' | 'white';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  onclick?: string;
  disabled?: boolean;
}

const {
  href,
  type = 'button',
  variant = 'primary',
  size = 'md',
  class: className = '',
  onclick,
  disabled = false,
} = Astro.props;

const baseClasses = 'font-semibold rounded transition-all duration-300 shadow-md hover:shadow-lg inline-flex items-center justify-center hover:scale-105 active:scale-95';

const variantClasses = {
  primary: 'bg-white text-secondaryDark border border-secondaryDark hover:bg-secondaryDark hover:text-white',
  secondary: 'bg-white text-secondary border-secondary border hover:bg-secondary hover:text-white',
  outline: 'bg-transparent border border-secondary text-secondary hover:bg-secondary hover:text-white',
  white: 'bg-white text-secondary hover:bg-white/80',
};

const sizeClasses = {
  sm: 'py-2 px-4 text-sm',
  md: 'py-2.5 px-5 text-sm',
  lg: 'py-3 md:py-4 px-6 md:px-8 text-lg',
};

const disabledClasses = disabled ? 'opacity-50 cursor-not-allowed pointer-events-none' : 'cursor-pointer';

const buttonClasses = `${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${disabledClasses} ${className}`;
---

{href ? (
  <a
    href={href}
    class={buttonClasses}
    class:list={disabled ? ['disabled'] : []}
  >
    <slot />
  </a>
) : (
  <button
    type={type}
    class={buttonClasses}
    class:list={disabled ? ['disabled'] : []}
    {onclick}
    disabled={disabled}
  >
    <slot />
  </button>
)}
