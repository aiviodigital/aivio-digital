---
interface Props {
  points: string[];
}

const { points } = Astro.props;
---

<div class="grid md:grid-cols-2 gap-6 mb-12">
  {points.map(point => (
    <div class="bg-gradient-to-br from-secondary/5 to-secondary/10 p-8 rounded border-l-4 border-secondary">
      <p class="text-lg text-gray-700 leading-relaxed">
        {point}
      </p>
    </div>
  ))}
</div>
