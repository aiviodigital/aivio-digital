---
interface Props {
  title: string;
  description?: string;
  items: Array<{
    key: string;
    icon: string;
    title: string;
    description: string;
  }>;
}

const { title, description, items } = Astro.props;
---

<div class="py-20 px-4 bg-gray-50">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
        {title}
      </h2>
      {description && (
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          {description}
        </p>
      )}
    </div>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {items.map(item => (
        <div class="bg-white p-8 rounded shadow-md hover:shadow-xl transition-all duration-300 group">
          <div class="w-14 h-14 bg-secondaryDark rounded-lg flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={item.icon}></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">
            {item.title}
          </h3>
          <p class="text-gray-600 leading-relaxed">
            {item.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</div>
