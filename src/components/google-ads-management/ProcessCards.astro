---
import Icon from '../common/Icon.astro';

interface Props {
  items: Array<{
    icon: string;
    title: string;
    description: string;
  }>;
  title: string;
  subtitle?: string;
}

const { items, title, subtitle } = Astro.props;
---

<section class="py-20 px-4 bg-gradient-to-br from-primary/5 to-white">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
        {title}
      </h2>
      {subtitle && (
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          {subtitle}
        </p>
      )}
    </div>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {items.map((item) => (
        <div class="bg-white p-8 rounded shadow-md hover:shadow-xl transition-all duration-300">
          <Icon><path d={item.icon} /></Icon>
          {/* <div class="w-12 h-12 bg-secondaryDark rounded-lg flex items-center justify-center mb-6">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={item.icon}></path>
            </svg>
          </div> */}
          <h3 class="text-lg font-bold text-secondary mt-6 mb-3">
            {item.title}
          </h3>
          <p class="text-gray-600 leading-relaxed">
            {item.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>
